# PDF Processing Dependencies
pdf2image>=3.1.0
opencv-python>=4.8.0
pillow>=10.0.0
numpy>=1.24.0
pytesseract>=0.3.10

# For Windows PDF processing (poppler)
# Download poppler from: https://github.com/oschwartz10612/poppler-windows/releases
# Extract and add to PATH, or install via conda: conda install -c conda-forge poppler

# For OCR (tesseract)
# Windows: Download from https://github.com/UB-Mannheim/tesseract/wiki
# Add tesseract.exe to PATH
# Or install via conda: conda install -c conda-forge tesseract
